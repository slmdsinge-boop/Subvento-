import { describe,expect,it } from "vitest";import { DOSSIER_STATUSES,DISPOSITIF_TRACKING_STATUSES,dossierProgress,dossierTotals,type Dossier } from "./dossier";import { emptyApplication } from "./schema";
function makeDossier(overrides:Partial<Dossier>={}):Dossier{return {id:"d1",project:{...emptyApplication.project_details},status:"brouillon",createdAt:new Date().toISOString(),submittedAt:null,dispositifs:[],checkedItems:[],...overrides}}
describe("workflow des statuts",()=>{it("le statut de dossier couvre l'intégralité du cycle demandé",()=>{for(const s of ["brouillon","pret","depose","en_instruction","accepte","refuse","a_justifier","cloture"])expect(DOSSIER_STATUSES).toContain(s)});it("le statut par dispositif couvre le cycle complet section 13",()=>{for(const s of ["brouillon","incomplet","a_valider","pret","depot_en_cours","depose","en_instruction","complement_demande","accepte","refuse","versement_en_cours","a_justifier","cloture"])expect(DISPOSITIF_TRACKING_STATUSES).toContain(s)})});
describe("dossierProgress",()=>{it("un brouillon est à 0%",()=>expect(dossierProgress(makeDossier({status:"brouillon"}))).toBe(0));it("un dossier clôturé est à 100%",()=>expect(dossierProgress(makeDossier({status:"cloture"}))).toBe(100));it("un dossier accepté est très avancé sans être fictivement à 100%",()=>expect(dossierProgress(makeDossier({status:"accepte"}))).toBe(90))});
describe("dossierTotals",()=>{it("additionne les montants réels, jamais une estimation inventée",()=>{const dossier=makeDossier({dispositifs:[{dispositifId:"CNM_PROD_PHONO",status:"accepte",montantDemande:5000,montantPercu:3000},{dispositifId:"ADAMI_AIDE_PROJET",status:"en_instruction",montantDemande:1200,montantPercu:null}]});expect(dossierTotals(dossier)).toEqual({demande:6200,percu:3000})});it("retourne 0 quand aucun dispositif n'a de montant",()=>expect(dossierTotals(makeDossier())).toEqual({demande:0,percu:0}))});